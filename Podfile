# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'StyleTransfer-iOS-TFlite' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StyleTransfer-iOS-TFlite
  pod 'TensorFlowLiteSwift'
  target 'StyleTransfer-iOS-TFliteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StyleTransfer-iOS-TFliteUITests' do
    # Pods for testing
  end

end
